#include <bits/stdc++.h>

const int N = 1000005;

int T, n, x, y;
int a[N];

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);
    std::cin >> T;
    while (T--) {
        std::cin >> x >> y;
        std::cin >> n;
        for (int i = 1; i <= n; ++i) std::cin >> a[i];
        for (int i = 1; i <= n; ++i) {
            
        }
        std::cout <<  << std::endl;
    }
    return 0;
}