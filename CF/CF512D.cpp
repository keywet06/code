#include <bits/stdc++.h>
const int N = 100005;
int n, m, k, c1, c2, c3, t, x, y;
int a[N];
int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);
    std::cin >> n >> m >> k;
    for (int i = 1; i <= k; ++i) std::cin >> a[i];
    for (int i = 1; i <= n; ++i) {
        std::cin >> t >> x >> y;
        if (t == 1) {
            
        }
    }
    return 0;
}