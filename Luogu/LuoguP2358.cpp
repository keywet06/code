#include <bits/stdc++.h>
int d, n, x, y, z;
int main() {
    scanf("%d", &d);
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i) {
        scanf("%d %d %d", &x, &y, &z);
        x += 10;
        y += 10;
        
    }
    return 0;
}