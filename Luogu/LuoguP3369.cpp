#include <bits/stdc++.h>

template <typename DataType, typename IntVector = std::vector<int>,
          typename DataVector = std::vector<DataType> >
class Splay {
   public:
    class node {
       public:
    };
};