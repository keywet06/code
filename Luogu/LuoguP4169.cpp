#include <bits/stdc++.h>

const int N = 2000005;

int n, m;
int ri[N], rx[N], ry[N], rz[N], rn[N];

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0), std::cout.tie(0);
    std::cin >> n >> m;
    for (int i = 1; i <= n; ++i) std::cin >> rx[i] >> ry[i], rz[i] = 1;
    return 0;
}