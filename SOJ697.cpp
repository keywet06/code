#include <bits/stdc++.h>
const int N = 3005;
int n;
int a[N], l[N], r[N], k[N];
int main() {
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i) {
        scanf("%d", &a[i]);
    }
    
    return 0;
}