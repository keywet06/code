#include <bits/stdc++.h>
const int N = 1000005;
int m, n, x;
int a[N], b[N], c[N], p[N];
int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    std::cout.tie(0);
    std::cin >> n;
    for (int i = 1; i <= n; ++i) std::cin >> a[i];
    a[0] = a[n];
    a[1] = a[n + 1];
    for (int i = 0; i < n; ++i) {
        for ()
    }
    std::cin >> m;
    for (int i = 1; i <= m; ++i) {
        std::cin >> x;
        std::cout << c[x] << '\n';
    }
    return 0;
}